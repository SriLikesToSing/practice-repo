#include <stdio.h>
#include <stdlib.h>

struct BstNode{
    int data;
    struct BstNode *left;
    struct BstNode *right;


};

BstNode *create_node(int data);
{
    BstNode = *new_node = (node*)malloc(sizeof(node));
    if(new_node == NULL){
        fprintf()
    }
}
